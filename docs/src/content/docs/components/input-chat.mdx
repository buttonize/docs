---
title: Input.chat
description: Input.chat displays an input field for chat.
---

import { TabItem, Tabs } from '@astrojs/starlight/components'
import ComponentDemo from '@components/ComponentDemo.astro'
import FrameworkSelect from '@components/FrameworkSelect.astro'
import FrameworkContent from '@components/FrameworkContent.astro'
import PropDescription from '@components/PropDescription.astro'
import PropMarker from '@components/PropMarker.astro'

<FrameworkSelect>
	<FrameworkContent>
		<Fragment slot="cdk">
			[Source](https://github.com/buttonize/buttonize-cdk/blob/master/src/components/input/chat.ts)
		</Fragment>
	</FrameworkContent>
</FrameworkSelect>

Displays an input field that can be used to interact with a chatbot.

```ansi frame="none"
[38;5;39mInput.chat[0m(props)
```

## Usage

<FrameworkContent>
   <Fragment slot="cdk">
    ```ts title="lib/MyStack.ts" collapse={1-5}
    import { ButtonizeApp, Input } from '@buttonize/cdk'

    new ButtonizeApp(this, 'DemoApp', {
      name: 'Demo App'
    }).page('DemoPage', {
      body: [
        Input.chat({
          id: 'chatInput',
          onMessage: {
            type: 'sendMessage',
            content: 'Hello, World!'
          },
          placeholder: 'Type your message...',
        })
      ]
    })
    ```

   </Fragment>
</FrameworkContent>

## Preview

{/* TODO: fill this in properly */}

<ComponentDemo
	components={[
		{
			typeName: 'input.chat',
			props: {
				id: 'chatInput',
				onMessage: {
					type: 'aws',
					region: 'eu-central-1',
					service: 'bedrock',
					command: 'invoke',
					content: 'Hello, World!'
				},
				placeholder: 'Type your message...'
			}
		}
	]}
/>

## Props

<FrameworkContent>
	<Fragment slot="cdk">
		<PropDescription>
			<Fragment slot="title">### id</Fragment>
			<PropMarker slot="markers" type="required" />
			<Fragment slot="type">string</Fragment>
			<Fragment slot="description">TBD</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">### onMessage</Fragment>
			<PropMarker slot="markers" type="required" />
			<Fragment slot="type">function</Fragment>
			<Fragment slot="description">
				A function to invoke when the user submits a message
			</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">### placeholder</Fragment>
			<PropMarker slot="markers" type="optional" />
			<Fragment slot="type">string</Fragment>
			<Fragment slot="description">Placeholder for the input field.</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">### disabled</Fragment>
			<PropMarker slot="markers" type="optional" />
			<Fragment slot="type">boolean</Fragment>
			<Fragment slot="description">
				Whether or not the input is disabled. Default is `false`.
			</Fragment>
		</PropDescription>
	</Fragment>
</FrameworkContent>
