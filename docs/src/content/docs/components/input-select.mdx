---
title: Input.select
description: Input.select displays a input field.
---

import { TabItem, Tabs } from '@astrojs/starlight/components'
import ComponentDemo from '@components/ComponentDemo.astro'
import FrameworkSelect from '@components/FrameworkSelect.astro'
import FrameworkContent from '@components/FrameworkContent.astro'
import PropDescription from '@components/PropDescription.astro'
import PropMarker from '@components/PropMarker.astro'

import SpacingProps from '../../../components/SpacingProps.mdx'
import SizingProps from '../../../components/SizingProps.mdx'

<FrameworkSelect>
	<FrameworkContent>
		<Fragment slot="cdk">
			[Source](https://github.com/buttonize/buttonize/blob/master/cdk/components/input/select.ts)
		</Fragment>
	</FrameworkContent>
</FrameworkSelect>

Displays a dropdown field, allowing a user to select an option from a pre-defined list.

```ansi frame="none"
[38;5;39mInput.select[0m(props)
```

## Usage

<FrameworkContent>
   <Fragment slot="cdk">
    ```ts title="lib/MyStack.ts" collapse={1-4} {"since the id of the input is selectInput, access the option's values like so":18}
    import { ButtonizeApp, Input, Display } from 'buttonize/cdk'

    new ButtonizeApp(this, 'DemoApp')
      .page('DemoPage', {
        body: [
          Input.select({
            id: 'selectInput',
            options: [
              { label: 'Option 1', value: 'option1' },
              { label: 'Option 2', value: 'option2' },
            ],
            label: 'Select an option',
            placeholder: 'Choose one...',
            initialValue: [
              { label: 'Option 1', value: 'option1' }
            ],
          }),

          Display.text("Selected label: {{selectInput.label}}"),
          Display.text("Selected value: {{selectInput.value}}")
        ]
      })
    ```

   </Fragment>
</FrameworkContent>

## Preview

<ComponentDemo
	height={200}
	components={[
		{
			typeName: 'input.select',
			props: {
				id: 'selectInput', // the name of the variable in the runtime state
				options: [
					{ label: 'Option 1', value: 'option1' },
					{ label: 'Option 2', value: 'option2' }
				],
				label: 'Select an option',
				placeholder: 'Choose one...',
				initialValue: [{ label: 'Option 1', value: 'option1' }]
			}
		},
		{
			typeName: 'display.text',
			props: {
				label: 'Selected label: {{selectInput.label}}'
			}
		},
		{
			typeName: 'display.text',
			props: {
				label: 'Selected value: {{selectInput.value}}'
			}
		}
	]}
/>

## Runtime State

The value in the runtime state is the selected object from the select input. You can use dot notation to
access the desired property for either the label or the value.

```ts title="lib/MyStack.ts" "'name'"
Input.select({
	id: 'selectInput',
	options: [
		{ label: 'Option 1', value: 'option1' },
		{ label: 'Option 2', value: 'option2' }
	],
	label: 'Select an option'
})
```

```json title="Runtime state (1st option selected)" frame="terminal" ""name""
// Assuming the first option is the selected value
{
	"selectInput": {
		"label": "Option 1",
		"value": "option1"
	}
}
```

```json title="Runtime state (2nd option selected)" frame="terminal" ""name""
// If the section option is the selected value, it would of course be
{
	"selectInput": {
		"label": "Option 2",
		"value": "option2"
	}
}
```

As demonstrated by the Usage example above, you can access variables from the Runtime State
via the "\{\{selectInput.value\}\}" syntax from elsewhere in your Buttonize app.

## Props

<FrameworkContent>
	<Fragment slot="cdk">
		<PropDescription>
			<Fragment slot="title">
				{/* keep-good-prettier-formatting */}
				### id
			</Fragment>
			<PropMarker slot="markers" type="required" />
			<Fragment slot="type">string</Fragment>
			<Fragment slot="description">
				The name of the variable containing the selected option. This variable
				can then be referenced elsewhere in your Buttonize app.
			</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">
				{/* keep-good-prettier-formatting */}
				### options
			</Fragment>
			<PropMarker slot="markers" type="required" />
			<Fragment slot="type">\{ label: string; value: string | number | boolean \}[]</Fragment>
			<Fragment slot="description">
				An array of options. Each option should have a `label` and `value` key
				set.
			</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">
				{/* keep-good-prettier-formatting */}
				### label
			</Fragment>
			<PropMarker slot="markers" type="optional" />
			<Fragment slot="type">string</Fragment>
			<Fragment slot="description">
				Text to display above the select input.
			</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">
				{/* keep-good-prettier-formatting */}
				### disabled
			</Fragment>
			<PropMarker slot="markers" type="optional" />
			<Fragment slot="type">boolean</Fragment>
			<Fragment slot="description">
				Whether or not the input is disabled. Default is `false`.
			</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">
				{/* keep-good-prettier-formatting */}
				### placeholder
			</Fragment>
			<PropMarker slot="markers" type="optional" />
			<Fragment slot="type">string</Fragment>
			<Fragment slot="description">
				Placeholder text for the select input.
			</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">
				{/* keep-good-prettier-formatting */}
				### multi
			</Fragment>
			<PropMarker slot="markers" type="optional" />
			<Fragment slot="type">boolean</Fragment>
			<Fragment slot="description">
				Enables multi-select functionality. Default is `false`.
			</Fragment>
		</PropDescription>
		<PropDescription>
			<Fragment slot="title">
				{/* keep-good-prettier-formatting */}
				### initialValue
			</Fragment>
			<PropMarker slot="markers" type="optional" />
			<Fragment slot="type">\{ label: string; value: string | number | boolean \}</Fragment>
			<Fragment slot="description">
				Initial selected value or values for the select input. Use an array for
				multi-select.
			</Fragment>
		</PropDescription>
		<SpacingProps />
		<SizingProps />
	</Fragment>
</FrameworkContent>
